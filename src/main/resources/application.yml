server:
  port: 8080
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/sbmd?allowMultiQueries=true&useUnicode=true&characterEncoding=UTF-8&useSSL=false&serverTimezone=CTT
    username: root
    password: 123
    driver-class-name: com.mysql.jdbc.Driver
mybatis:
  type-aliases-package: com.dazhi.sbmd
  mapper-locations: classpath:/mapper/*Mapper.xml

